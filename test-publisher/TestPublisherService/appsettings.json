{
  "rabbitConfig": {
    "adress": "localhost",
    "port": 5672,
    "user": "guest",
    "password": "guest"
  },
  "rabbitTransactionConfig": {
    "adress": "localhost",
    "port": 5673,
    "user": "guest",
    "password": "guest"
  },
  "serviceInfo": {
    "name": "transport"
  },
  "transportConsumer": {
    "exchange": "resources/transport",
    "queue": "transport_queue",
    "routing": "query"
  }
}
